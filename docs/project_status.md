# 项目状态

## 当前可展示

- README 首屏与真实看板截图。
- 统一的 6 视图 ECharts 在线入口。
- 一键 DuckDB 数仓构建与 12 组 SQL 结果导出。
- 指标字典、数据字典、四个业务案例和质量报告。
- 混合日期格式修复及回归测试。
- 代码、数据和分析结论的边界说明。

## 公开仓库已发布

- 已在独立环境执行 `python run_project.py` 和 `python -m pytest`。
- 已对全仓执行姓名、学号、绝对路径、密钥和大文件扫描。
- 已按 TfL、Open-Meteo 和 GOV.UK 来源增加公开数据署名与非背书声明。
- 已配置 GitHub Actions，在每次推送时重建数仓、看板并运行测试。
- 已完成 242 个源批次审计，并将 82 个时钟差异拆分为零骑行、源缺口和夏令时三类。
- 已将 4 个不完整源日期从业务分析视图和看板排除，同时保留原始观测层。
- 已完成 242 个批次的全量 OD 流式聚合：58,311,048 条 OD 有效行程、632,144 个 OD 组合和 888 个候选规范站点。
- 已生成站点别名、全量 OD、站点时段和同站行为聚合表，并接入 DuckDB、SQL 与测试。
- 已完成全量“流向结构”和“站点诊断”页签；旧样本站点页退出主导航。
- 公开仓库：`https://github.com/Ju-Wikun/london-bike-sharing-analysis`。
- 首次 GitHub Actions `quality` 工作流已通过。

## 已知但不阻塞后续迭代的问题

- 旧课程站点页仍保留为演进证据，但主导航与当前结论均使用全量聚合表。
- 站点候选键基于规范化名称，93 个候选站点存在源 ID 复用风险，仍需官方历史站点表或坐标复核。
- GitHub Pages 已于 2026-08-26 发布：https://ju-wikun.github.io/london-bike-sharing-analysis/ 。部署包只包含六个活跃视图和固定版本 ECharts 运行库。

## 下一增强项

- 生成 `fact_station_hourly`，补充站点流入、流出、净流量和连续失衡 SQL。
- 将行程级展示样例改成可复现的分层抽样文件，并记录抽样清单与哈希。
- 制作两页 Power BI：运营总览、站点诊断。
- 每次 main 更新先运行构建与测试，再自动部署 Pages；继续保留站点映射限制，不增加无关 Agent/RAG。
